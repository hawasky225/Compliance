import Link from"next/link";import{requireAdmin}from"@/lib/admin";import{prisma}from"@/lib/prisma";
export default async function AdminCertificationsPage(){await requireAdmin();const schemes=await prisma.certificationScheme.findMany({include:{_count:{select:{certifications:true,assessments:true}},certifications:{select:{status:true,expiresAt:true}}},orderBy:{code:"asc"}});const total=schemes.reduce((n,s)=>n+s._count.certifications,0);return <main className="page-shell"><div className="admin-page-head"><div><p className="eyebrow">GOUVERNANCE DES CERTIFICATIONS</p><h1>Schémas de certification</h1><p>Définissez les standards, seuils, validités et règles de délivrance Compliance.</p></div><button className="primary-btn">＋ Nouveau schéma</button></div><div className="directory-kpis"><div><b>{schemes.length}</b><span>Schémas</span></div><div><b>{schemes.filter(s=>s.active).length}</b><span>Actifs</span></div><div><b>{total}</b><span>Certificats délivrés</span></div><div><b>{schemes.reduce((n,s)=>n+s._count.assessments,0)}</b><span>Examens rattachés</span></div></div><div className="scheme-grid">{schemes.map(s=><article className="scheme-card" key={s.id}><div className="scheme-top"><span className="scheme-code">{s.code}</span><span className={`access-pill ${s.active?"active":"disabled"}`}>{s.active?"Actif":"Inactif"}</span></div><h3>{s.title}</h3><p>{s.description||"Aucune description."}</p><div className="scheme-metrics"><div><span>VALIDITÉ</span><b>{s.validityMonths} mois</b></div><div><span>SEUIL</span><b>{s.passingScore}%</b></div><div><span>TENTATIVES</span><b>{s.maxAttempts}</b></div></div><div className="scheme-footer"><span>{s._count.assessments} examen(s)</span><span>{s._count.certifications} certificat(s)</span><button className="table-action">Configurer →</button></div></article>)}</div><div className="governance-note"><div>◈</div><div><b>Gouvernance de certification</b><p>Prochaine étape : approbation des schémas, suspension/révocation, renouvellement, prérequis et piste d’audit complète.</p></div><Link href="/admin/utilisateurs" className="outline-btn">Gérer les rôles</Link></div></main>}
